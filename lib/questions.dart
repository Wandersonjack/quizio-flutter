class Question {
  String questionText;
  bool questionAnswer;

  //constructor with named param
  Question({String question, bool answer}){
    questionText = question;
    questionAnswer = answer;
  }
}